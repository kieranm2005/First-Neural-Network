'''For a given trail, this script visualizes the data from each episode, highlighting the most common paths taken by the agent. It shows this data as a heatmap the size of the trail's grid'''
import matplotlib.pyplot as plt
import numpy as np
import os
import json
